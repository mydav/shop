<% if line_item == @current_item %>
<li id="current_item" class="list-group-item">
<% else %>
<li class="list-group-item">


<% end %>

    <%= line_item.quantity %>&times;

    <%= line_item.product.title %>
    <span class="badge"><%= number_to_currency(line_item.total_price) %></span>
  </li>
